import { GetTimeCommentPipe } from './get-time-comment.pipe';

describe('GetTimeCommentPipe', () => {
  it('create an instance', () => {
    const pipe = new GetTimeCommentPipe();
    expect(pipe).toBeTruthy();
  });
});
